name: Deploy to GitHub Pages

on:
  push:
    branches: [ "master" ]  # твоя основная ветка

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Забираем код из репозитория
      - name: Checkout code
        uses: actions/checkout@v4

      # 2️⃣ Устанавливаем Node.js и кэшируем pnpm
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      # 3️⃣ Устанавливаем pnpm через официальный action
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      # 4️⃣ Проверяем, что pnpm доступен
      - name: Check pnpm version
        run: pnpm -v

      # 5️⃣ Устанавливаем зависимости
      - name: Install dependencies
        run: pnpm install

      # 6️⃣ Строим проект
      - name: Build project
        run: pnpm run build

      # 7️⃣ Проверяем наличие dist/index.html
      - name: Verify build contents
        run: |
          echo "=== dist folder contents ==="
          ls -la dist/
          if [ -f "dist/index.html" ]; then
            echo "✅ index.html found!"
          else
            echo "❌ index.html NOT found!"
            exit 1
          fi

      # 8️⃣ Настраиваем GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v3

      # 9️⃣ Загружаем собранный артефакт
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

      # 🔟 Деплой на GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2

