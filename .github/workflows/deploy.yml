name: Deploy to GitHub Pages

# Запуск при пуше в основную ветку
on:
  push:
    branches: [ "master" ]  # Если ваша основная ветка main — заменить на main

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Забираем код
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Устанавливаем Node.js >=22 (Vite >=7 требует)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      # 3️⃣ Устанавливаем pnpm глобально
      - name: Install pnpm globally
        run: npm install -g pnpm

      # 4️⃣ Проверяем что pnpm доступен
      - name: Verify pnpm
        run: pnpm -v

      # 5️⃣ Устанавливаем зависимости проекта
      - name: Install dependencies
        run: pnpm install

      # 6️⃣ Строим проект (vite build создаёт dist)
      - name: Build project
        run: pnpm run build

      # 7️⃣ Проверяем наличие dist/index.html
      - name: Verify build contents
        run: |
          echo "=== dist folder contents ==="
          ls -la dist/
          if [ -f "dist/index.html" ]; then
            echo "✅ index.html found!"
          else
            echo "❌ index.html NOT found!"
            exit 1
          fi

      # 8️⃣ Настраиваем GitHub Pages
      - name: Setup Pages
        uses: actions/configure-pages@v3

      # 9️⃣ Загружаем собранный артефакт
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

      # 🔟 Деплой на GitHub Pages
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
